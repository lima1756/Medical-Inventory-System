import React from 'react';

function NoMatch() {
    return (
        <div>
            <h1>La pagina que busca no existe aqui, usar el menu superior para regresar</h1>
        </div>
    );
}

export default NoMatch;